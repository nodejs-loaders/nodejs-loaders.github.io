import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	// not now lasted thing todo
	// it's require the https://nextjs.org/docs/app/building-your-application/routing/dynamic-routes#generating-static-params
	//output: 'export',
};

export default nextConfig;
